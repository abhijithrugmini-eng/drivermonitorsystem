# Design — <change-id>

## Approach

<!-- The technical approach in prose. Reference the relevant dms-*-dev skill(s) and, if this touches an agent, .claude/skills/dms-agentic-architecture/SKILL.md's BaseAgent contract explicitly. -->

## Architecture / flow

<!--
If the change has non-trivial data/control flow, replace this comment with a real ```mermaid fenced block (e.g. graph LR; A[...] --> B[...]) — generate_release.py reuses it verbatim in release.md's Change Flow section. Leave this as a comment (no live ```mermaid fence) if there's nothing worth diagramming; the release script falls back to a component-pipeline diagram on its own.
-->

## Files touched

<!-- New files, modified files, one line each on what changes. -->

## Data / API contract changes

<!-- New/changed schemas, endpoints, agent input-output shapes. Call out anything that breaks an existing dms-spec/specs/*/spec.md requirement. -->

## Alternatives considered

<!-- Only if explore.md didn't already cover this in enough depth. -->

## Risks / open questions
