export default function StubPage({ title }) {
  return (
    <div className="stub-page">
      <h2>{title}</h2>
      <p>Coming soon in a future phase — the POC demo script only exercises Overview.</p>
    </div>
  );
}
